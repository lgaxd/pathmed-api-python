# PathMed API (FastAPI)

Uma API RESTful para gerenciamento de consultas médicas e registros de pacientes do sistema PathMed, portada para Python e FastAPI.

## 🛠 Tecnologias

- Python 3.10+
- FastAPI
- Oracle Database (via `oracledb`)
- Pydantic
- JWT (para autenticação)

## 🚀 Instalação

1. Clone o repositório.
2. Crie e ative um ambiente virtual:
   ```bash
   python -m venv venv
   source venv/bin/activate  # (Linux/macOS)
   .\venv\Scripts\activate   # (Windows)